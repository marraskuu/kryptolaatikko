# Railway Variables (web-palvelu)
SECRET_KEY=generoi-pitka-satunnainen-avain
DEBUG=false
ALLOWED_HOSTS=*

# MySQL — Railway linkittää automaattisesti, tai viittaa MySQL-palveluun:
# DATABASE_URL=${{MySQL.MYSQL_URL}}

# Paikallinen MySQL (valinnainen, ilman tätä käytetään sqlitea)
# DATABASE_URL=mysql://root:salasana@127.0.0.1:3306/kryptolaatikko
